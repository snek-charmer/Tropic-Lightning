apiVersion: v2
name: keycloak-portal
description: Go OIDC portal that authenticates via Keycloak and enforces Keycloak roles
type: application
# Chart version — bump on chart changes.
version: 0.1.0
# Version of the app this chart deploys (matches the container image tag default).
appVersion: "0.1.0"
keywords:
  - keycloak
  - oidc
  - portal
  - istio
sources:
  - https://github.com/snek-charmer/Tropic-Lightning
